namespace Abort {
    public exception aborting(string);

    public void abort(string reason) {
	raise aborting(reason);
    }

    public void assert(bool ok, string failmsg) {
	if (!ok)
	    abort(failmsg);
    }
}
